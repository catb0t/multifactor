#! sidef -kP12

var regex = /(string)/

const z = "a"==1
var z = "a" == 1

say z
__END__
const a = "a\nstring\nok".lines \
  .find { |line| line =~ regex } \ # =~ regex
  .match(regex)

const b = regex=~("b\nstring\nnok".lines \
  .find { |line| line =~ regex })
  # .match(regex)

