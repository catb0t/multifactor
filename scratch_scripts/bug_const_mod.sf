module F {
  define VALUE = "any value"
}

# works
say "works "+(nil || F::VALUE)

class A {
  method f {
    # works
    say "works "+F::VALUE
    say "fails "+(nil ||F::VALUE)
  }
}

func x {
  say "works "+F::VALUE
  say "fails "+(nil ||F::VALUE)
}

var a = A()
a.f
